Read `FinalArchitecture.md` fully.

1. Find the first unchecked task (`- [ ]`).
2. Before executing, check the previous completed tasks and existing code for consistency.
3. Execute ONLY that one task. Output only the files/code needed.
4. Verify: compiles, no conflicts, naming matches existing code, imports correct.
5. Mark it `- [x]` in `FinalArchitecture.md`. Do not touch any other task.
6. If that was the last task in the current STEP, generate a verification report covering what was built, how it connects to prior steps, and confirmation that the full step works end-to-end.
   - **Backend tasks (B-X):** Save to `reports/backend/STEP-B-X.md`.
   - **Frontend tasks (F-X):** Save to `reports/frontend/STEP-F-X.md`.

Stop after one task.
